// const findEmail = require('./findUserBy').byEmail;
// const LocalStrategy = require('passport-local').Strategy;
// const bcrypt = require('bcrypt');

// const initialize = (passport, email) => {
//         const dbEmail = findEmail(email);

//         if (dbEmail === null) done(null, false, console.log('No user found'))

//         try {
//             if (await bcrypt.compare(password, user.password)) done(null, user)
//             else done(null, false, 'Password incorrect')
//         } catch (error) {
//             return done(error);
//         }
//     };

//     passport.use(
//         new LocalStrategy({
//             usernameField: 'email'
//         }),
//         authenticateUser
//     )
//     passport.serializeUser(user, done) => { })
//     passport.deserializeUser(id, done) => { })
};




